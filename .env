# Committed on purpose: nothing in this file is secret.
# Anything prefixed VITE_ is bundled into the JavaScript that ships to players,
# so it is public by definition. Secrets belong in *.local files, which git ignores.

# The game's display title. This is the only place to change it (docs/decisions.md, D7).
VITE_GAME_TITLE=Under New Management
